import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    heading: {
        fontSize: 22,
        color: '#3740FE',
        textAlign: 'center',
        marginTop:10
    }
});

export {styles}